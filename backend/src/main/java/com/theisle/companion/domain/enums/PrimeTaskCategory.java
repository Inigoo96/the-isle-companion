package com.theisle.companion.domain.enums;

public enum PrimeTaskCategory {
    // DEFAULT maps to "default" in PostgreSQL (toLowerCase() handles the reserved-word gap)
    DEFAULT, DIET_BIRTH, EXPLORATION, BREEDING, SPECIAL_SPECIES
}
